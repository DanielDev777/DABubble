# Normalize line endings to LF in the repo so files run correctly in Linux
# containers regardless of the host OS. Prevents CRLF from breaking shell
# scripts, Dockerfiles, and entrypoints.
* text=auto eol=lf

# Binary files git should never touch
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.pdf binary
